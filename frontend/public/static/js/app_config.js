// Frontend app configuration

// Protected/backend API base (same-origin Express mounts at /api)
window.APP_API_BASE = window.APP_API_BASE || '/api';

// Optional: external data API base (only if you intentionally fetch public data from external host)
window.DATA_API_BASE = window.DATA_API_BASE || 'https://api.sharadaresearch.in';

